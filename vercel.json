{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm build",
  "installCommand": "corepack enable && corepack prepare pnpm@9.5.0 --activate && pnpm install --frozen-lockfile",
  "regions": ["hnd1"],
  "github": {
    "silent": false,
    "autoAlias": true
  }
}